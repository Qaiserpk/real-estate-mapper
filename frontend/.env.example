# Copy to .env and add your free MapTiler key to enable vector tiles + language labels.
# Get one at https://www.maptiler.com/  (Account -> API keys)
# Without this, the app falls back to raster OpenStreetMap (native-language labels only).
VITE_MAPTILER_KEY=
